-# The :php include used to name text.html.php#language, a repository key VS Code's php grammar
-# does not have; vscode-textmate then dropped the whole rule and the region went unscoped.
:php
  echo "hello";
  $x = 1;
%p after the filter
